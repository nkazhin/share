# Archived OZON generator pages

Archived on 2026-06-17.

These GitHub Pages HTML tools were removed from the public root after the client version moved to the password-gated Cloud Run app at `https://ozon.etopodtema.com`.

The archived files use `.html.txt` extensions on purpose, so GitHub Pages does not serve them as runnable tools.

Do not restore these files to the GitHub Pages root unless the replacement has billing, admin test mode, and current password handling.
